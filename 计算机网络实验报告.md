# 计算机网络基础知识实验报告

## 一、实验目的

1. 掌握 Linux 网络配置方法，使主机接入互联网
2. 熟练使用 Wireshark / tcpdump 等抓包工具
3. 通过 ping 抓包分析 DNS、ARP、ICMP 协议的工作过程
4. 搭建双机通信环境，通过 nc 抓包理解 TCP 三次握手及报文格式

## 二、实验环境

| 项目 | 说明 |
|------|------|
| 虚拟化 | VMware Workstation 17 / VirtualBox 7.0 |
| 虚拟机 OS | Ubuntu 22.04 LTS |
| 网络工具 | netplan, iproute2 |
| 抓包工具 | Wireshark, tcpdump |
| 其他工具 | ping, nc (netcat), ss |

## 三、实验原理

### 3.1 TCP/IP 协议栈

```
应用层      HTTP, DNS, SSH
传输层      TCP, UDP
网络层      IP, ICMP, ARP
链路层      Ethernet, Wi-Fi (802.11)
```

数据封装：应用层消息 → TCP/UDP 头 → IP 头 → 以太网帧头 → 物理层比特流。

### 3.2 IP 报文格式（IPv4，头部 20 字节）

| 字段 | 位数 | 说明 |
|------|------|------|
| 版本 | 4 | IPv4=4 |
| 首部长度 | 4 | 以 4 字节为单位，最小 5 |
| 总长度 | 16 | 头部+数据总长度 |
| TTL | 8 | 每跳减 1，归零丢弃 |
| 协议 | 8 | 1=ICMP, 6=TCP, 17=UDP |
| 源 IP | 32 | 发送方 IP 地址 |
| 目的 IP | 32 | 接收方 IP 地址 |

### 3.3 TCP 报文格式（头部 20 字节）

| 字段 | 位数 | 说明 |
|------|------|------|
| 源端口 | 16 | 发送方端口 |
| 目的端口 | 16 | 接收方端口 |
| 序列号 (SEQ) | 32 | 本报文段数据首字节序号 |
| 确认号 (ACK) | 32 | 期望收到的下一个序号 |
| 标志位 | 6 | URG/ACK/PSH/RST/**SYN**/**FIN** |
| 窗口大小 | 16 | 流量控制窗口 |

关键标志位：**SYN**（建立连接）、**ACK**（确认）、**FIN**（释放连接）、**RST**（重置）。

### 3.4 TCP 三次握手

```
Client                              Server
  |                                    |
  |── SYN (seq=x, SYN=1) ────────────▶|  ① 客户端请求建立连接
  |                                    |
  |◀─ SYN+ACK (seq=y, ack=x+1) ──────|  ② 服务器确认并响应
  |                                    |
  |── ACK (seq=x+1, ack=y+1) ───────▶|  ③ 客户端确认，连接建立
  |                                    |
  ├──── 连接建立 (ESTABLISHED) ────────┤
```

**为什么是三次？** 三次握手能确保双方收发能力均正常，同时防止已失效的连接请求到达服务器导致资源浪费。两次不够（服务器无法确认客户端收到了自己的 SYN），四次多余。

### 3.5 ICMP 与 ARP

- **ICMP**（协议号 1）：网络诊断协议，Type=8 为 Echo Request（ping 发出），Type=0 为 Echo Reply
- **ARP**：通过 IP 地址解析 MAC 地址——广播请求 + 单播响应

## 四、实验步骤与结果分析

### 任务 1：配置 Linux 联网

```bash
# 查看网卡和 IP
ip addr show

# 编辑 netplan 配置（Ubuntu 22.04）
sudo vim /etc/netplan/00-installer-config.yaml
```

```yaml
# DHCP 方式（NAT 模式适用）
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:
      dhcp4: true
```

```bash
# 应用配置
sudo netplan apply

# 验证连通性
ping -c 4 baidu.com
```

**结果**：VMware NAT 模式下，虚拟 DHCP 自动分配 IP 和网关，安装完系统即可上网。ping 成功说明链路层、IP 层、DNS 解析均正常。

### 任务 2：抓包工具使用

```bash
# tcpdump 监听网卡
sudo tcpdump -i ens33 -nn -w capture.pcap

# 读取 pcap 文件
tcpdump -r capture.pcap -nn

# Wireshark 图形界面
wireshark &
```

**关键概念**：抓包工具将网卡设为**混杂模式**，接收所有经过的帧（而非仅发给自己的）。

### 任务 3：Ping 百度并分析抓包

```bash
# 终端1：抓包
sudo tcpdump -i ens33 -nn -w ping_baidu.pcap

# 终端2：执行 ping
ping -c 4 baidu.com
```

**抓包分析（按时间顺序）**：

**① DNS 查询**（UDP，端口 53）：客户端向 DNS 服务器发送 Query，查询 `baidu.com` 的 A 记录；服务器返回 Reply，包含解析出的 IP 地址（如 `110.242.68.66`）。

**② ARP 解析**：若目标 IP 不在本地子网，主机需向网关发送数据包。先广播 ARP Request（"谁是网关？"），网关单播 ARP Reply（告知 MAC 地址）。ARP 表可通过 `arp -a` 查看。

**③ ICMP Echo Request**（Type=8）：封装顺序为 `Ethernet[网关MAC] → IP[Proto=1, Src=本机, Dst=baidu] → ICMP[Type=8] → Data`。

**④ ICMP Echo Reply**（Type=0）：百度服务器回复，TTL 值会因经过路由器而减少。ping 输出 RTT 统计（min/avg/max）。

**注意**：首次 ping 的 RTT 较大，因为前面需完成 DNS 和 ARP 解析。

### 任务 4：nc 建立 TCP 连接并分析三次握手

#### 4.1 使用 namespace + veth pair 搭建双机环境（轻量级方案）

```bash
# 创建两个网络命名空间
sudo ip netns add ns-A
sudo ip netns add ns-B

# 创建 veth pair 并分配
sudo ip link add veth-a type veth peer name veth-b
sudo ip link set veth-a netns ns-A
sudo ip link set veth-b netns ns-B

# 配置 IP 并启用
sudo ip netns exec ns-A ip addr add 10.0.0.1/24 dev veth-a
sudo ip netns exec ns-A ip link set veth-a up
sudo ip netns exec ns-B ip addr add 10.0.0.2/24 dev veth-b
sudo ip netns exec ns-B ip link set veth-b up

# 验证互通
sudo ip netns exec ns-A ping -c 2 10.0.0.2
```

#### 4.2 nc 通信与抓包

```bash
# 终端1：ns-B 监听
sudo ip netns exec ns-B nc -l -p 9999

# 终端2：抓包
sudo ip netns exec ns-A tcpdump -i veth-a -nn -w tcp_handshake.pcap port 9999

# 终端3：ns-A 发起连接
sudo ip netns exec ns-A nc 10.0.0.2 9999
```

#### 4.3 三次握手抓包分析

在 Wireshark 中打开 `tcp_handshake.pcap`，可看到三个报文：

**第一次握手（SYN）：`10.0.0.1 → 10.0.0.2`**

| 字段 | 值 | 含义 |
|------|-----|------|
| SYN Flag | 1 | 连接请求 |
| Sequence Number | ISN_C | 客户端随机初始序号 |
| Options | MSS=65495 | 协商最大报文段大小 |

**第二次握手（SYN+ACK）：`10.0.0.2 → 10.0.0.1`**

| 字段 | 值 | 含义 |
|------|-----|------|
| SYN Flag | 1 | 服务器也发送 SYN |
| ACK Flag | 1 | 确认客户端的 SYN |
| Sequence Number | ISN_S | 服务器随机初始序号 |
| Acknowledgment Number | ISN_C + 1 | 确认消耗了一个序号 |

**第三次握手（ACK）：`10.0.0.1 → 10.0.0.2`**

| 字段 | 值 | 含义 |
|------|-----|------|
| SYN Flag | 0 | 连接已建立 |
| ACK Flag | 1 | 确认服务器的 SYN |
| Sequence Number | ISN_C + 1 | |
| Acknowledgment Number | ISN_S + 1 | 确认服务器的 SYN 被消耗 |

三次报文完成后双方进入 **ESTABLISHED** 状态，后续 `nc` 终端中输入的文本将作为 TCP 数据段传输。

### 任务 5：清理环境

```bash
sudo ip netns del ns-A
sudo ip netns del ns-B
```

## 五、实验总结

### 完成情况

| 任务 | 结果 |
|------|:---:|
| Linux 网络配置（netplan） | ✅ |
| 抓包工具使用（tcpdump/Wireshark） | ✅ |
| ping 协议栈分析（DNS→ARP→ICMP） | ✅ |
| TCP 三次握手抓包验证 | ✅ |

### 核心收获

1. **Linux 网络配置**：掌握 netplan YAML 配置、`ip` 命令族（`ip addr/link/route`）、DNS 配置
2. **抓包分析**：掌握 Wireshark/tcpdump 基本用法和 BPF 过滤，理解抓包对网络排障的重要性
3. **IP 报文格式**：理解版本、总长度、TTL、协议等关键字段的含义和作用
4. **TCP 三次握手**：理解 SYN→SYN+ACK→ACK 的过程和状态转换，明白"为什么是三次"
5. **TCP 报文格式**：理解源/目的端口、序列号、确认号、6 个标志位、窗口大小等字段

---

> **实验日期**：2026 年 6 月 2 日
